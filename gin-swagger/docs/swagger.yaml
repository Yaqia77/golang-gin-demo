basePath: /api/v1
host: localhost:8080
info:
  contact: {}
  description: This is a sample server for MyApp.
  title: MyApp API
  version: "1.0"
paths:
  /users:
    get:
      consumes:
      - application/json
      description: Get all users
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              type: string
            type: array
      summary: Get all users
      tags:
      - users
swagger: "2.0"
